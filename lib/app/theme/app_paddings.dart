import 'package:flutter/material.dart';

class AppPaddings {
  static const EdgeInsets screen = EdgeInsets.all(24.0);
  static const EdgeInsets card = EdgeInsets.all(32.0);
  static const EdgeInsets dialog = EdgeInsets.symmetric(
    horizontal: 24,
    vertical: 28,
  );
  static const EdgeInsets button = EdgeInsets.symmetric(vertical: 16);
  static const EdgeInsets listItem = EdgeInsets.symmetric(
    horizontal: 16,
    vertical: 8,
  );
}

class AppSpacings {
  static const double xSmall = 4.0;
  static const double small = 8.0;
  static const double medium = 16.0;
  static const double large = 24.0;
  static const double xLarge = 32.0;
}
